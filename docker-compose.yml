services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: tripos
      POSTGRES_PASSWORD: tripos
      POSTGRES_DB: tripos
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: